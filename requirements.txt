# ============================================================
# MACHINE: Intel Iris GPU (integrated) — CPU-only torch required
# PyTorch GPU acceleration needs NVIDIA CUDA only. Intel Iris = CPU mode.
#
# INSTALL ORDER for fresh setup:
#   1. pip install torch --index-url https://download.pytorch.org/whl/cpu
#   2. pip install -r requirements.txt
#
# This installs the ~200MB CPU torch instead of the ~2.5GB GPU version.
# All project code runs correctly on CPU — no GPU needed.
# ============================================================

# ============================================================
# Core Agent Framework
# ============================================================
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# ============================================================
# Vector Database & Embeddings
# ============================================================
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ============================================================
# Data Handling
# ============================================================
pandas>=2.1.0
numpy>=1.26.0

# ============================================================
# Config & Utilities
# ============================================================
python-dotenv>=1.0.0

# ============================================================
# Evaluation & Metrics
# ============================================================
scikit-learn>=1.3.0

# ============================================================
# HTTP & API
# ============================================================
requests>=2.31.0
httpx>=0.27.0

# ============================================================
# Logging & Serialization
# ============================================================
jsonlines>=4.0.0
