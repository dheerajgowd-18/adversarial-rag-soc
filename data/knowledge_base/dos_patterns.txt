DOCUMENT_ID: kb_dos_001
CATEGORY: attack_pattern
ATTACK_TYPE: dos
TITLE: DoS Attack Variants - Flow-Level Signatures and Detection

## Overview
Denial of Service (DoS) attacks originate from a SINGLE source and aim to exhaust
target resources. Unlike DDoS (distributed), DoS flows often have more distinguishable
per-flow signatures because the attack volume comes from one machine.

## DoS Variants Present in CICIDS2017

### DoS Hulk
Target: Web servers (port 80/443)
Method: Generates unique HTTP GET requests per connection to bypass caching
Signature:
  - High volume of TCP flows to port 80 from single source
  - Each flow: 6-12 packets, ~12KB total
  - flow_bytes_per_sec: varies widely (10–5,300,000 B/s)
  - packet_length_mean: ~800 bytes (large, mimics real HTTP)
  - Long flow duration: 7–120 seconds

### DoS Slowloris
Target: Web servers with limited thread pools
Method: Holds connections open by sending partial HTTP headers slowly
Signature:
  - VERY LONG flow duration (minutes, not seconds)
  - Very few packets (3–8 forward)
  - Near-zero throughput (bytes/sec very low)
  - RST or FIN flags present late in flow
  - dst_port: 80 or 443

### DoS GoldenEye
Target: Web servers
Method: HTTP keepalive + random cache-control headers
Signature:
  - Moderate packet count (10–30 forward)
  - High total_bytes (10–15KB per flow)
  - dst_port: 80 or 443
  - Consistent 1-second inter-arrival times

### DoS Slowhttptest
Target: Web servers with connection limits
Method: Slow POST body — sends data at 1 byte per second
Signature:
  - Extremely long duration (minutes)
  - Tiny throughput
  - dst_port: 80
  - Forward packets: 10–50, backward: 5–20

## CICIDS2017 DoS Statistical Profile

From real Wednesday pcap data:
  flow_duration_us:    median 84,880,200 (84 seconds — very long flows!)
  fwd_packets:         median 6 (range 1–12)
  bwd_packets:         median 6 (range 0–8)
  total_bytes:         median 11,924 bytes
  packet_length_mean:  median 794 bytes
  flow_bytes_per_sec:  median 121 B/s (very slow — Slowloris-like)

KEY INSIGHT: The VERY LOW flow_bytes_per_sec (median 121 B/s) combined with
VERY LONG flow duration (84 seconds) is the signature of Slowloris/Slowhttptest.
Compare to benign median: 5,367 B/s over 32ms. DoS holds connections 2600x longer.

## Detection Logic

Flow is likely DoS if ALL of:
  1. dst_port in {80, 443, 8080}          — targeting web server
  2. flow_duration_us > 10,000,000        — connection held > 10 seconds
  3. total_bytes between 5,000–15,000     — typical HTTP-sized payload
  4. fwd_packets between 4–15             — limited genuine data exchange
  5. packet_length_mean > 400             — large-ish packets (HTTP headers)

Flow is HIGHLY SUSPICIOUS if additionally:
  6. flow_bytes_per_sec < 500             — extremely slow data transfer
     (benign web traffic is 5,000+ B/s)

## Distinguishing DoS from Benign Web Traffic

The clearest separator is the combination of:
  LONG DURATION + LOW THROUGHPUT + WEB PORT

A legitimate HTTPS session to load a webpage takes 30-500ms.
A DoS Slowloris connection holds open for 30-300 seconds with near-zero data.
No legitimate use case maintains a web connection this long with this little data.

## Triage Guidance

Classify as SUSPICIOUS (DoS) if:
- Web port target AND duration > 30 seconds AND throughput < 500 B/s
- Multiple simultaneous connections from same source to same web server
- Connection duration orders of magnitude above site's normal baseline

## Severity Assessment
  Severity: HIGH to CRITICAL
  Impact: Service degradation to complete outage
  Recommended action: Rate limit source IP, notify upstream, check server logs
